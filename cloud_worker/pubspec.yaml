name: riskform_cloud_worker
description: Cloud Run backtest worker service for RiskForm

version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  # Reference to main project for engine code
  # In production, you may want to extract shared code to a separate package
  path: ^1.9.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
