name: riskform_cloud_worker
description: Cloud Run backtest worker service for RiskForm

version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  # Local shared core package containing engines and models.
  riskform_core:
    path: ../packages/riskform_core

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
